# Environment Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3000
NODE_ENV=development

# Note: All sensitive data (API keys, webhooks) are stored in the database
# and managed through the admin interface - NOT in environment variables
